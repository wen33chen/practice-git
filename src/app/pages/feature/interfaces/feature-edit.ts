export interface FeatureEdit {
  featureId: number;
  featureCode: string;
  featureOldName: string;
  featureName: string;
  featureGroupId: any;
  isActive: boolean;
  isAllowAssignRoleUse: boolean;
  sort: number;
}
