export interface FeatureData {
  featureId: number;
  featureCode: string;
  featureName: string;
  featureGroupId: any;
  featureGroupName: string;
  isActive: boolean;
  isAllowAssignRoleUse: boolean;
  sort: number;
}
